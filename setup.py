from setuptools import setup

PLUGIN_NAME="cm_plugin_hello"

setup(
    name=PLUGIN_NAME,
    packages=[PLUGIN_NAME],
    version="0.0.10",
    description="A POC for importing packages",
    url="www.example.com",
)
